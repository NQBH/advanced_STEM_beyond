#include <bits/stdc++.h>
using namespace std;
const int m = 998244353;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	int t;
	cin >> t;
	while (t--) {
		int n;
		cin >> n;
		vector<int> a(n), f(n + 1, 0);
		for (int &x : a) {
			cin >> x;
			++f[x];
		}


		
	}
}
// unfinished