#include <iostream>
#include <vector>
using namespace std;
using ll = long long;

int main() {
	vector<bool> is_power2(1 << 50, false);
	for (int i = 1; i <= 50; ++i) is_power2()
	int t;
	cin >> t;
	while (t--) {
		ll n;
		if (n % 2) cout << "YES\n";
		else 

	}
}