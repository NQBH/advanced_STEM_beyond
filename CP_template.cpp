#include <bits/stdc++.h>
using namespace std;
int main() {
	// input & output
	ios::sync_with_stdio(0);
	cin.tie(0);
	// handle input/output files
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
	while (cin >> x) {
		// code
	}

	return 0;
}