#include <iostream>
#include <vector>

int main() {
	int t, m, n;
	cin >> t;
	while (t--) {
		cin >> n >> m;
		vector<int> a(n), b(n);
		for (int& v : a) cin >> a;
		for (int& v : b) cin >> b;
		
	}

}