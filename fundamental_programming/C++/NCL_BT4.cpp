#include <iostream>
#include <cmath>
using namespace std;

int main () {
 double x;
 cin >> x;
 if (x < 0) cout << "x must be nonnegative";
 else cout << sqrt(x) << '\n';
 
 double y;
 cin >> x >> y;
 if (x < 0) cout << "x dương";
 else cout << pow(x,y) << '\n';
 
 cin >> x;
 if (x < 0) cout << "x dương";
 else cout << exp(x) << '\n';
 
 cin >> x;
 if (x < 0) cout << "x dương";
 else cout << log(x) << '\n';
 
 cin >> x;
 if (x < 0) cout << "x dương";
 else cout << log10(x) << '\n';
  
 cin >> x;
 if (x < 0) cout << "x dương";
 else cout << abs(x) << '\n';
 
 cin >> x;
 if (x < 0) cout << "x dương";
 else cout << labs(x) << '\n';
 
 cin >> x;
 if (x < 0) cout << "x dương";
 else cout << fabs(x) << '\n';
 
 cin >> x;
 if (x < 0) cout << "x dương";
 else cout << cos(x) << '\n';
 
 cin >> x;
 if (x < 0) cout << "x dương";
 else cout << sin(x) << '\n';
 
 cin >> x;
 if (x < 0) cout << "x dương";
 else cout << tan(x) << '\n';
 
 cin >> x;
 cout << ceil(x) << '\n';
 
 cin >> x;
 cout << floor(x) << '\n';
 
 
 }
  